# _PORTRAIT_001 — seed frozen snapshot (TEMPLATE)

> **TEMPLATE / seed. Zero personal content — placeholders only.**
> Go live: `cp _PORTRAIT_001.template.md _PORTRAIT_001.md`, then fill on your
> first real `/portrait`. Live `_PORTRAIT_*.md` files are gitignored;
> this `.template.md` seed stays tracked. (Scheme: `../../.gitignore`.)
>
> **What a snapshot is:** a FROZEN, dated, immutable read of the portrait at one
> moment — the same primitive as a `_NEXT_NNN.md` handoff. **Never edit a snapshot
> after it's written.** The next repaint writes `_PORTRAIT_002.md`; it does not
> touch this one. The **becoming-arc is the diff between consecutive snapshots** —
> which is only meaningful if each one is frozen. (CLAUDE.md: "frozen once written";
> "archive before mutating".)

---

snapshot: 001
frozen: <YYYY-MM-DD>
corpus rev: sources.md @ <hash-or-date>
prior snapshot: none — this is the seed
disputes reconciled into this snapshot: none — seed
synthesized by: /portrait (manual seed for 001)

---

## 1. Stable identity
- <stable trait — placeholder>  [✓VERIFIED] (confidence: high)
  ev: <source-id> → "<quote>"

## 2. Becoming / growth themes
- <theme — placeholder>  [?INFERRED] (confidence: low)
  ev: <source-id> → "<locator>"   ← note: 001 is the seed; no prior snapshot to diff,
                                     so growth themes here are single-point reads. The
                                     arc becomes real at _PORTRAIT_002 (first diff).

## 3. Unresolved tensions
- <tension — placeholder>  [?INFERRED] (confidence: medium)
  ev: <source-id> → "<locator>"

## 4. Operator-patterns
- <pattern from the Throughline loop — placeholder>  [✓VERIFIED] (confidence: medium)
  ev: next/_NEXT_<NNN>.md → "<conviction/outcome locator>"

---

## Seed note
This is the baseline. It establishes the first frozen reference point; every later
snapshot's "becoming" diff is measured against the chain starting here. Do not
backfill or rewrite it later — if early reads were wrong, that's visible *as* the
diff to _PORTRAIT_002, which is the honest record. Frozen means frozen.
